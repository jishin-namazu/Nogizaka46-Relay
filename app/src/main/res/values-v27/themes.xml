<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- A style declared in a qualified folder replaces the default bag instead of merging with
         it, so the launch theme repeats every item. Without them API 27-30 dropped
         android:windowBackground and showed a blank splash instead of launch_background. -->
    <style name="Theme.NogiRelay.Launch" parent="android:style/Theme.Material.Light.NoActionBar">
        <item name="android:fontFamily">sans</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowActionModeOverlay">true</item>
        <item name="android:windowBackground">@drawable/launch_background</item>
        <item name="android:statusBarColor">@color/splash_background</item>
        <item name="android:navigationBarColor">@color/splash_background</item>
        <item name="android:windowLightStatusBar">true</item>
        <item name="android:windowLightNavigationBar">true</item>
    </style>

    <style name="Theme.NogiRelay.Call" parent="@style/Theme.NogiRelay.CallBase">
        <item name="android:windowLightNavigationBar">true</item>
        <item name="android:windowLayoutInDisplayCutoutMode">shortEdges</item>
    </style>
</resources>
